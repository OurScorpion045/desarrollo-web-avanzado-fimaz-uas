En esta actividad cree la clase Usuario que tiene como atributos Nombre y correo, despues cree la clase Admin que es heredada de Usuario por lo tanto esta nueva clase tiene los atributos de su clase padre y ademas tiene un nuevo metodo llamado getRol() que devuelve administrador, la unica diferencia entre la clase Usuario y Admin es que Admin tiene el metodo getRol() que Usuario no posee