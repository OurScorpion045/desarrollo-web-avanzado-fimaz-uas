En esta practica cree como clase padre la clase Usuario del archivo Usuario.php donde se tomaron los parametros de $nombre y $correo para despues mediante un condicional if y un throw new Exception se validara que el parametro $correo tuviera el formato de correo correcto y se convirtiera en el atributo this->correo o en caso contrario se lanzara una excepcion con un mensaje de error, este condicional de excepcion se uso tanto en el constructor como en el setter del correo de la clase para comprobar que el nuevo correo a establecer tambien tenga en formato correcto, en el archivo Admin.php cree la clase Admin hija de Usuario que incluye un metodo extra llamado getRol() que retorna "Administrador", en el archivo Alumno.php cree la clase Alumno hija de la clase Usuario la cual incluye un nuevo atributo llamado matricula y un nuevo metodo llamado getRol() que importa "Alumno", en el archivo index.php se importan los archivos Admin.php y Alumno.php para utilizar sus respectivas clases, en el archivo cree 2 funcionees para validar la informacion de tanto usuarios de tipo alumno como de tipo admin, ambas funciones realizan lo mismo, usan un try catch para intentar validar al usuario y en caso de que el correo este incorrecto el bloque catch muestra la excepcion, el unico añadido de la funcion validarAlumno es que tambien valida el atributo $this->matricula de la clase Alumno